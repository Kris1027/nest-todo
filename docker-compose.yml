services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - DATABASE_URL=file:./prisma/dev.db
    volumes:
      - todo-data:/app/prisma
    restart: unless-stopped

volumes:
  todo-data:
